---
import BlogCard from '../components/BlogCard.astro';
import { posts } from '../data/posts';
---

<h1 class="text-3xl font-bold text-center my-8">Garden Blog</h1>
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto px-4">
  {posts.map(post => (
    <BlogCard 
      title={post.title}
      image={post.image}
      date={post.date}
      excerpt={post.excerpt}
      slug={post.slug}
    />
  ))}
</div>

<a href="https://affiliate-link.com/tool" rel="nofollow sponsored" target="_blank" class="mt-10 mx-auto block w-max bg-green-700 hover:bg-green-800 text-white px-6 py-3 rounded-lg shadow transition-colors text-lg font-semibold">
  🌿 Buy Gardening Tools
</a>